# Testing Platform — Environment Variables
# Copy to your project root as `.env.test` and customize.

# Required
TEST_MODE=true
BASE_URL=http://localhost:3000
DB_PATH=./data/test.sqlite

# Optional
SCREENSHOT_DIR=./test/screenshots
TRACE_DIR=./test/traces
METRIC_ENDPOINT=

# Test data tagging
TEST_TAG=is_test
